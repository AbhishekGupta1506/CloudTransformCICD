pipeline {
	agent { label none }
    stages{
      stage('Install OnPremise Designer') {
       agent {
        label CTP
       }
       steps{
        echo "hello Designer"
       }
      }
    }
 }